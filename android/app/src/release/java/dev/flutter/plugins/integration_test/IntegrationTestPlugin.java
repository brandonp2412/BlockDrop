package dev.flutter.plugins.integration_test;

import androidx.annotation.NonNull;
import io.flutter.embedding.engine.plugins.FlutterPlugin;

/**
 * Release-build stub. The real IntegrationTestPlugin is only available on the
 * debug/test classpath (via the Flutter SDK AAR). GeneratedPluginRegistrant
 * references this class unconditionally, so we provide a no-op stand-in to
 * satisfy the compiler. It is registered but immediately does nothing.
 */
public class IntegrationTestPlugin implements FlutterPlugin {
    @Override
    public void onAttachedToEngine(@NonNull FlutterPluginBinding binding) {}

    @Override
    public void onDetachedFromEngine(@NonNull FlutterPluginBinding binding) {}
}
